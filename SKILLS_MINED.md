# Skills Mined — `skill-mining`

> Generated by the **skill-mining** skill on `2026-07-20`. Scope: `.`. HEAD: `3d7d819cb0ec2f2b7c529349a57d2a71198494c9`.

## Summary

- Candidates considered: **18**
- Built: **1** · Reused: **0** · Extended: **1** · Rejected: **6** · Deferred: **10**
- Agents composed: **0**

## Coverage (what this pass surveyed)

- Files surveyed: **40**; source files sampled for convention detection: **11**
- Pain markers (TODO/FIXME/HACK) found: **6**
- Bound: prompt file list capped at 300 paths (full tree summary included)
- Bound: source samples capped at 12 files × 4000 chars
- Bound: churn/bug-fix windows: last 500 commits
- Bound: CI workflow files capped at 5
- Bound: stale skill directories on disk, not produced by this run (review/remove manually): ecosystem-skill-deduplication, gate-b-red-team-verification, release

## Candidate ledger

Every candidate considered, with score and decision. Nothing dropped silently.
Scores shown are *post–Gate A* (the independent skeptic's blind re-score; Δ is
the proposer-vs-skeptic disagreement — high Δ is signal).

| Candidate | Type | Freq | Lev | Bsp | Stab | Ver | Δ | Evidence | Decision | Gate A objection / Source |
|---|---|:--:|:--:|:--:|:--:|:--:|:--:|---|---|---|
| deterministic-checks-not-llm | skill | 4 | 4 | 3 | 4 | 5 | 5 | ✓ | **EXTEND** | The core principle is plausibly already covered by jpcaparas/skills@heuristic-to-deterministic (40 installs) in the ecosystemSearch output — the generic 'replace model heuristics with deterministic checks' idea is not bespoke. What IS bespoke and worth keeping is the repo-specific enforcement layer: RISK_RE word-anchored routing, Set-lookup evidence verification, and the fail-closed rule that deterministic findings are handed to LLM reviewers as non-overrulable confirmed defects. Extend the existing skill with those invariants rather than building a parallel statement of the general principle. |
| safe-subprocess-invocation | skill | 4 | 5 | 5 | 4 | 5 | 3 | ✓ | **BUILD** | survived: registry search returned literally 'No skills found' for the primary query and only an irrelevant hit (warpdotdev@readout) for the secondary, so REUSE is off the table; evidence is verified and src/dedupe.js has 2 bug-fix commits proving the gotchas were paid for twice. The Windows .cmd shim forcing shell:true (making sanitizeQuery-before-winQuote ordering load-bearing) and the cold-npx-timeout-launders-into-BUILD failure are exactly the class of cross-platform subprocess traps a competent agent re-hits — I could not refute leverage or bespokeness. |
| survey-exclusion-and-git-semantics | skill | 2 | 3 | 5 | 3 | 4 | 2 | ✓ | **DEFER** | Recurrence is not proven: the out-dir self-contamination gotcha happened once and is now mechanically guarded by buildSurveyExcludes — the code itself enforces the invariant, so a skill only pays off when someone next modifies the survey. Three TODO markers are intentions, not recurrence, and the SURVEY_CAPS coupling is a single-file checklist item. Ecosystem hits (typing-exclusion-worker, placement-exclusion-manager) are name-collision noise, so REUSE is wrong, but the evidence today is one incident plus anticipation. |
| adversarial-gate-mechanics | skill | 3 | 3 | 5 | 3 | 4 | 1 | ✓ | **REJECT** | The candidate cites skill-mining/references/adversarial-review.md as its own evidence — this knowledge is ALREADY authored, in-repo, as the canonical reference the gates ship with. Mining a second artifact restating blind payloads, veto rules, and MAX_FIX_ROUNDS creates two sources of truth for gate invariants, and the reference file's own churn (2 changes, 1 bug-fix) guarantees the mined copy drifts stale. The correct move is pointing at the existing reference, not duplicating it; a duplicate that silently diverges from the real voting rules is worse than no skill. |
| release-and-publish-recipe | skill | 3 | 4 | 2 | 4 | 5 | 1 | ✓ | **REJECT** | The evidence refutes the build case itself: .claude/commands/release.md and .claude/release-profile.md already exist in-repo and encode exactly this tag-driven OIDC flow — the recipe is captured, and this is a risk-relevant (publish) procedure where a duplicate skill diverging from the real command is the worst failure mode available. Ecosystem coverage is also broad (thedotmack/claude-mem@claude-code-plugin-release at 4.1K installs, nvidia/skills@nemoclaw-maintainer-cut-release-tag) for the generic tag/publish shape. Reuse the existing in-repo command + profile; fold the PACKAGE_VERSION runtime-read gotcha into release-profile.md if it is missing there. |
| pipeline-reviewer | agent | 3 | 4 | 4 | 4 | 3 | - | ✓ | **REJECT** | failed agent gate (cold-load or lint) |
| fail-closed-reuse-search-policy | skill | 2 | 5 | 5 | 4 | 3 | - | ⚠ unverified | **DEFER** | n/a |
| cli-flag-three-place-sync | skill | 2 | 4 | 5 | 3 | 3 | - | ⚠ unverified | **DEFER** | n/a |
| dogfood-own-skill-artifacts | skill | 2 | 4 | 5 | 4 | 5 | - | ⚠ unverified | **DEFER** | n/a |
| npm-package-files-whitelist | skill | 2 | 4 | 4 | 4 | 4 | - | ⚠ unverified | **DEFER** | n/a |
| llm-json-contract-conventions | skill | 2 | 4 | 4 | 3 | 3 | - | ⚠ unverified | **DEFER** | n/a |
| report-sidecar-contract | skill | 2 | 4 | 5 | 3 | 3 | - | ⚠ unverified | **DEFER** | n/a |
| llm-provider-abstraction-rules | skill | 2 | 4 | 3 | 2 | 3 | - | ⚠ unverified | **DEFER** | n/a |
| pipeline-implementer | agent | 4 | 3 | 3 | 4 | 4 | - | ✓ | **DEFER** | this repo |
| pipeline-fixer | agent | 2 | 3 | 3 | 4 | 4 | - | ⚠ unverified | **DEFER** | this repo |
| release-operator | agent | 2 | 2 | 3 | 4 | 5 | - | ✓ | **REJECT** | this repo |
| how-this-repo-runs-and-tests | skill | 2 | 2 | 2 | 4 | 5 | - | ⚠ unverified | **REJECT** | n/a |
| node-test-authoring-conventions | skill | 2 | 2 | 2 | 4 | 3 | - | ⚠ unverified | **REJECT** | n/a |

## Built / reused skills — with identity

| Skill | Origin | Path | Source / version pin | Content fingerprint (whole dir) | Verification |
|---|---|---|---|---|---|
| `deterministic-checks-not-llm` | EXTEND | `.agents/skills/deterministic-checks-not-llm/` | jpcaparas/skills@heuristic-to-deterministic | `sha256:77023c7109efb031693c981dcb1a4ddc2d39c8509f91098d50caa3de5a71156c` | **Gate B** @ 2026-07-20: used cold vs "Harden the skill-mining pipeline by adding deterministic enforcement around the adversarial gates: verify cited evidence paths with Set lookups in src/evidence.js, route candidates to the risk gate via word-anchored RISK_RE keyword matching in src/gates.js, and ensure all deterministic findings fail closed and are passed to LLM reviewers as non-overridable confirmed defects." (real commit 65e834c) → SHIP; fixes: none |
| `safe-subprocess-invocation` | BUILD | `.agents/skills/safe-subprocess-invocation/` | this repo | `sha256:4cbfdf25b06aaeb17af2ac482c3c491998d77e8fa1185308ba0b4dfec90fe59d` | **Gate B** @ 2026-07-20: used cold vs "Add a local Codex CLI fallback to src/llm.js that invokes `codex exec` as a child process using execFile with an argv array (no shell strings), guards command detection against the Windows .cmd shim charset issue, ignores stderr noise, applies a timeout, and closes stdin to prevent hangs." (real commit b17b42b) → SHIP; fixes applied (1 round(s)): Step 6 mandates using execCliIgnoringStderr from src/llm.js but never documents its signature or contract: a cold agent cannot tell whether it accepts a timeout option, whether it closes the child's stdin internally, or what it returns (stdout string? object?). The test task requires 'applies a timeout' and 'closes stdin' — following the skill literally, the agent cannot verify either requirement is satisfied when routing through this helper, and may either omit the timeout entirely or double-handle stdin.; Step 5 (always call child.stdin.end()) and Step 6 (use execCliIgnoringStderr, whose stdio is ['pipe','pipe','ignore'] so stdin is a pipe) are in unresolved tension: the skill never states whether the helper ends stdin for you. A missing stdin.end() 'presents as a timeout, not an error' per the skill's own warning, so this ambiguity reproduces exactly the failure class the skill exists to prevent.; The only timeout constants the skill names (WARMUP_TIMEOUT_MS=60000, SEARCH_TIMEOUT_MS=15000) belong to src/dedupe.js and are semantically about npx skills searches. For the codex-exec call in src/llm.js the agent must invent a timeout value and constant name with no guidance — LLM CLI invocations plausibly need a much larger budget than 15s, and the Conventions section ('every subprocess call has an explicit timeout') gives no number for this call class. |

**Reuse-check status (required for BUILT/EXTENDED rows):**

- `reuse-checked: deterministic checks not llm / adding verification gate routing report rendering via npx skills find @ 2026-07-20T22:13:33.936Z`
- `reuse-checked: safe subprocess invocation / spawning child process this codebase npx via npx skills find @ 2026-07-20T22:13:33.938Z`

Per-skill **file manifest** (the recorded file list partial modes compare against;
the whole-directory fingerprint is the hash of this manifest):

```
# deterministic-checks-not-llm — manifest @ 2026-07-20T22:20:55.266Z
SKILL.md   sha256:3b84ce85a67fda60972367c4e0a8d1076548053e79368dd71297da127bbf20d4
```

```
# safe-subprocess-invocation — manifest @ 2026-07-20T22:20:55.267Z
SKILL.md   sha256:23c6e8931faf1cf3116b2add8b50de17e2541c67981b9a84db3437d8ea5ed825
```

## Composed agents

*No agent roles survived composition and the cold-load gate.*

## Team manifest

*No team manifest (no agents composed).*

## Reused / Extended

- **`deterministic-checks-not-llm`** (extended) ← `jpcaparas/skills@heuristic-to-deterministic`

## Deferred — next mining pass

- **`survey-exclusion-and-git-semantics`** — Recurrence is not proven: the out-dir self-contamination gotcha happened once and is now mechanically guarded by buildSurveyExcludes — the code itself enforces the invariant, so a skill only pays off when someone next modifies the survey. Three TODO markers are intentions, not recurrence, and the SURVEY_CAPS coupling is a single-file checklist item. Ecosystem hits (typing-exclusion-worker, placement-exclusion-manager) are name-collision noise, so REUSE is wrong, but the evidence today is one incident plus anticipation.. Revisit when: survey.js's TODOs are actually worked, or a second exclusion/caps regression (missed SURVEY_CAPS update, out-dir contamination recurrence) lands in commit history.
- **`fail-closed-reuse-search-policy`** — deferred. Revisit when: cited SKILLS_MINED.json sidecar evidence verifies in a re-run; alternatively fold this invariant into the deterministic-checks-not-llm skill rather than building a second overlapping one.
- **`cli-flag-three-place-sync`** — deferred. Revisit when: evidence re-verifies against the repo (the outDir/outDirExplicit cite is currently unconfirmed) or the next CLI flag change reproduces the three-place drift.
- **`dogfood-own-skill-artifacts`** — deferred. Revisit when: cited paths (SKILL.md, candidate-taxonomy.md, adversarial-review.md) verify deterministically in a re-run, or the next edit to skill-mining/SKILL.md trips lint again.
- **`npm-package-files-whitelist`** — deferred. Revisit when: a new top-level runtime directory is added (the failure mode only fires then), or the bin-alias evidence cite verifies; consider a one-line npm pack --dry-run check in CI instead of a skill.
- **`llm-json-contract-conventions`** — deferred. Revisit when: evidence cites verify deterministically, or the next new LLM-calling phase is added and re-derives these contracts.
- **`report-sidecar-contract`** — deferred. Revisit when: sidecar evidence verifies in a re-run, or the next change to the partial modes (--agents-only/--report-only) touches this contract again.
- **`llm-provider-abstraction-rules`** — deferred. Revisit when: provider APIs and model defaults hold stable for a quarter; if built, the skill must point at src/llm.js as the live source of truth rather than copying model names and fallback order that churn.
- **`pipeline-implementer`** — deferred. Revisit when: after the BUILD-band skills ship — if a generic implementer agent with those skills loaded still shows skill-discovery friction on 3+ tasks, promote to a dedicated agent.
- **`pipeline-fixer`** — deferred. Revisit when: evidence cites verify and 3+ more fix-shaped tasks recur after the underlying skills ship — a generic debugging agent loading those skills likely covers this role.

## Rejected — do not re-mine without new evidence

- **`adversarial-gate-mechanics`** — The candidate cites skill-mining/references/adversarial-review.md as its own evidence — this knowledge is ALREADY authored, in-repo, as the canonical reference the gates ship with. Mining a second artifact restating blind payloads, veto rules, and MAX_FIX_ROUNDS creates two sources of truth for gate invariants, and the reference file's own churn (2 changes, 1 bug-fix) guarantees the mined copy drifts stale. The correct move is pointing at the existing reference, not duplicating it; a duplicate that silently diverges from the real voting rules is worse than no skill.
- **`release-and-publish-recipe`** — The evidence refutes the build case itself: .claude/commands/release.md and .claude/release-profile.md already exist in-repo and encode exactly this tag-driven OIDC flow — the recipe is captured, and this is a risk-relevant (publish) procedure where a duplicate skill diverging from the real command is the worst failure mode available. Ecosystem coverage is also broad (thedotmack/claude-mem@claude-code-plugin-release at 4.1K installs, nvidia/skills@nemoclaw-maintainer-cut-release-tag) for the generic tag/publish shape. Reuse the existing in-repo command + profile; fold the PACKAGE_VERSION runtime-read gotcha into release-profile.md if it is missing there.
- **`pipeline-reviewer`** — failed agent gate (cold-load or lint)
- **`release-operator`** — Agent role composed of skills
- **`how-this-repo-runs-and-tests`** — Rejected during scoring/challenge
- **`node-test-authoring-conventions`** — Rejected during scoring/challenge

## How to install

```bash
# From this repo:
npx skills add voodootikigod/skill-mining
# or copy into your harness skills dir (see references/cross-harness.md)
```
